hazelcast:
  cluster-name: news-cache

  map:
    newsById:
      time-to-live-seconds: 600
      max-idle-seconds: 300

      eviction:
        eviction-policy: LRU
        max-size-policy: PER_NODE
        size: 1000